#include <cstdint>
#include <random>

std::uint32_t random_num(std::default_random_engine *);
std::uint32_t random_odd_num(std::default_random_engine *);
std::uint32_t random_n(std::default_random_engine *);
