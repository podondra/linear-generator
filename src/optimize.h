#include <stdint.h>

void optimized_loop(
        const uint32_t,
        const uint32_t,
        const uint32_t,
        const uint32_t,
        const uint32_t,
        const uint32_t *__restrict,
        const uint32_t *__restrict,
        float *__restrict,
        uint32_t *__restrict,
        uint32_t *__restrict,
        uint32_t *__restrict,
        uint32_t *__restrict
        );
